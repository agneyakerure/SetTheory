# Personal data never lives in the repo — it's in ~/Documents/SetTheory.
# Your library, your purchases, your notes, and your music stay on your machine.

# Bandcamp collection, resolved labels, the derived map, transcoded audio
.cache/

# Throwaway notes for ./dev.sh — never real data, never committed.
.devdata/

# Built macOS app bundle — regenerate with ./make-app.sh (bakes a machine path)
SetTheory.app/

# Legacy from the first prototype; the map now lives in .cache/
data.json
state.json

# Python / macOS cruft
__pycache__/
*.pyc
.DS_Store
